plugins {
    id("me.samuki.layers.presentation")
}

android {
    namespace = "me.samuki.feature.compilation.creation.presentation"
}

dependencies {
    testImplementation(project(":core:testing"))
}
